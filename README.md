# kubernetes-external-dns-node-connector

This is a connector for external-dns which retrieves external node ips to use in DNS records.

## Cloud Build

    gcloud builds submit --config cloudbuild.yaml .